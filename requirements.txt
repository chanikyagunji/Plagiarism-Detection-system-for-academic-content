Requirements.txt

 	streamlit==1.32.0
		scikit-learn==1.2.2
		pandas==2.1.2
		numpy==1.26.4
		scipy==1.11.3
		python-docx==0.8.11
		PyPDF2==3.0.0
		joblib==1.3.2
		tqdm==4.66.1
		typing-extensions==4.8.0

Why pin versions? 
Pinning ensures anyone who installs your project later will get the same, tested package versions so the app runs reliably during demo and grading.

